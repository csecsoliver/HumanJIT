# Port for the backend server (default: 3000)
PORT=3000

# CORS origin for the WebSocket server (default: http://localhost:5173)
# Use "*" only in development, specify exact origins in production
CORS_ORIGIN=http://localhost:5173